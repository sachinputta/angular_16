export class StudentEnrollment {
    id: number;
    fullName: string;
    dateOfBirth: string;  // You can use Date object if needed
    contactNumber: string;
    emailAddress: string;
    courseName: string;
    gradeBatch: string;
    enrollmentDate: string;  // You can use Date object if needed
    admissionNumber: string;
    totalFee: number;
    amountPaid: number;
    paymentDate: string;  // You can use Date object if needed
    paymentMode: string;
    balanceAmount: number;
    feeStatus: string;
}
